name: Superseded SDK Registry Closure

# Historical temporary workflow filename retained only until the alpha.2
# registry-evidence cleanup PR. Publication has moved to the npm Trusted
# Publisher identity in publish-sdk-alpha.yml.
on: {}

permissions:
  contents: read

jobs: {}
