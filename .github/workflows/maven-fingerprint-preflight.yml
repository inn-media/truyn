name: Maven signing fingerprint preflight

on:
  push:
    branches: [release/maven-central-tag-launch-20260923]
    paths: [.github/workflows/maven-fingerprint-preflight.yml]

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    environment: sdk-release
    steps:
      - name: Canonicalize and verify signing identity
        shell: bash
        env:
          PRIVATE_KEY: ${{ secrets.MAVEN_GPG_PRIVATE_KEY }}
          CONFIGURED_LONG_KEY: ${{ secrets.MAVEN_GPG_PUBLIC_LONG_KEY }}
        run: |
          set -Eeuo pipefail
          test -n "$PRIVATE_KEY"; test -n "$CONFIGURED_LONG_KEY"
          export KEYFILE="$RUNNER_TEMP/maven-private-key.asc"
          python - <<'PY'
          import os
          s=os.environ['PRIVATE_KEY'].strip(); begin='-----BEGIN PGP PRIVATE KEY BLOCK-----'; end='-----END PGP PRIVATE KEY BLOCK-----'
          bi=s.find(begin); ei=s.rfind(end); lines=s[bi:ei+len(end)].splitlines(); interior=[x.rstrip('\r') for x in lines[1:-1]]
          while interior and not interior[0].strip(): interior.pop(0)
          with open(os.environ['KEYFILE'],'w',encoding='ascii',newline='\n') as f: f.write('\n'.join([begin,'']+interior+[end,'']))
          PY
          unset PRIVATE_KEY
          export GNUPGHOME="$RUNNER_TEMP/maven-gpg-probe"; mkdir -p -m 700 "$GNUPGHOME"
          gpg --batch --quiet --import "$KEYFILE"; rm -f "$KEYFILE"
          echo 'TRUYN_MAVEN_GPG_IMPORT=PASS'
          fpr="$(gpg --batch --with-colons --list-secret-keys | awk -F: '/^fpr/{print toupper($10);exit}')"
          long_id="$(gpg --batch --with-colons --list-secret-keys | awk -F: '/^sec/{print toupper($5);exit}')"
          configured="$(printf '%s' "$CONFIGURED_LONG_KEY" | tr -d '[:space:]' | sed 's/^0[xX]//' | tr '[:lower:]' '[:upper:]')"
          test -n "$fpr"; test -n "$long_id"
          [[ "$configured" =~ ^[0-9A-F]+$ ]]
          test "$long_id" = "$configured" || [[ "$fpr" == *"$configured" ]]
          echo 'TRUYN_MAVEN_LONG_KEY_PIN=PASS'

          openpgp="$(curl -LsS -o /dev/null -w '%{http_code}' "https://keys.openpgp.org/vks/v1/by-fingerprint/$fpr" || true)"
          ubuntu="$(curl -LsS -o "$RUNNER_TEMP/ubuntu-key.asc" -w '%{http_code}' "https://keyserver.ubuntu.com/pks/lookup?op=get&search=0x$fpr" || true)"
          mit="$(curl -LsS -o "$RUNNER_TEMP/mit-key.asc" -w '%{http_code}' "https://pgp.mit.edu/pks/lookup?op=get&search=0x$fpr" || true)"
          echo "TRUYN_MAVEN_KEYSERVER_STATUS openpgp=$openpgp ubuntu=$ubuntu mit=$mit"
          visible=false
          if test "$openpgp" = 200; then visible=true; fi
          if test "$ubuntu" = 200 && grep -q '^-----BEGIN PGP PUBLIC KEY BLOCK-----' "$RUNNER_TEMP/ubuntu-key.asc"; then visible=true; fi
          if test "$mit" = 200 && grep -q '^-----BEGIN PGP PUBLIC KEY BLOCK-----' "$RUNNER_TEMP/mit-key.asc"; then visible=true; fi
          rm -f "$RUNNER_TEMP/ubuntu-key.asc" "$RUNNER_TEMP/mit-key.asc"
          test "$visible" = true
          echo "TRUYN_MAVEN_SIGNING_FINGERPRINT=$fpr"
          echo 'TRUYN_MAVEN_SIGNING_IDENTITY=PASS'
