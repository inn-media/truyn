diff --git a/.github/workflows/d200-acceptance.yml b/.github/workflows/d200-acceptance.yml
--- a/.github/workflows/d200-acceptance.yml
+++ b/.github/workflows/d200-acceptance.yml
@@ -5,12 +5,12 @@ on:
     paths: ['.github/workflows/d200-acceptance.yml']
 permissions: { contents: read, actions: read, id-token: write }
-concurrency: { group: truyn-d200-0718c78d-qualified-single-shot, cancel-in-progress: false }
+concurrency: { group: truyn-d200-b03c82be-qualified-single-shot, cancel-in-progress: false }
 env:
-  TESTED_COMMIT: 0718c78d41b6d935aaafc4eb928a6061e895ce90
-  TESTED_TREE_SHA: c6806a4090d7731ffca25f7f3899242519155c28
-  EXACT_MAIN_CI_RUN: '35184720843'
-  EXACT_MAIN_CODEQL_RUN: '35184720334'
+  TESTED_COMMIT: b03c82be03ced800df6ac78698cd364c6cc3fa91
+  TESTED_TREE_SHA: 289cb03e6c4b3921cce4a1e2343074845e55b773
+  EXACT_MAIN_CI_RUN: '35234507940'
+  EXACT_MAIN_CODEQL_RUN: '35234508207'
   AZURE_RESOURCE_GROUP: truyn
   NODES_PER_HOST: '10'
@@ -88,7 +88,7 @@ jobs:
         id: upload
         if: always()
         uses: actions/upload-artifact@v4
         with:
-          name: 'truyn-class-d200-0718c78d-${{ github.run_id }}'
+          name: 'truyn-class-d200-b03c82be-${{ github.run_id }}'
           path: |
             class-d-200-*.json
             class-d-200-*.txt
diff --git a/tests/public-repository.test.js b/tests/public-repository.test.js
--- a/tests/public-repository.test.js
+++ b/tests/public-repository.test.js
@@ -43,7 +43,7 @@ test('published benchmark evidence is preserved and not replaced by stubs', asy
 test('temporary D-1000 launcher workflows are never committed to the public workflow tree',async()=>{const files=await collect();const temporaryLaunchers=files.map(f=>f.relative).filter(r=>r.startsWith('.github/workflows/tmp-class-d1000-')).sort();assert.deepEqual(temporaryLaunchers,[],`Temporary D-1000 launchers must remain execution scaffolding only:\n${temporaryLaunchers.join('\n')}`)});
-test('D-200 acceptance launcher is frozen to the qualified exact-main evidence', async()=>{const content=await readFile(path.join(ROOT,'.github/workflows/d200-acceptance.yml'),'utf8');for(const marker of ['TESTED_COMMIT: 0718c78d41b6d935aaafc4eb928a6061e895ce90','TESTED_TREE_SHA: c6806a4090d7731ffca25f7f3899242519155c28',"EXACT_MAIN_CI_RUN: '35184720843'","EXACT_MAIN_CODEQL_RUN: '35184720334'",'truyn-d200-0718c78d-qualified-single-shot'])assert.ok(content.includes(marker),`D-200 launcher lost qualified freeze marker: ${marker}`)});
+test('D-200 acceptance launcher is frozen to the qualified exact-main evidence', async()=>{const content=await readFile(path.join(ROOT,'.github/workflows/d200-acceptance.yml'),'utf8');for(const marker of ['TESTED_COMMIT: b03c82be03ced800df6ac78698cd364c6cc3fa91','TESTED_TREE_SHA: 289cb03e6c4b3921cce4a1e2343074845e55b773',"EXACT_MAIN_CI_RUN: '35234507940'","EXACT_MAIN_CODEQL_RUN: '35234508207'",'truyn-d200-b03c82be-qualified-single-shot'])assert.ok(content.includes(marker),`D-200 launcher lost qualified freeze marker: ${marker}`)});
 test('public repository contains no known operational/cloud leakage or credential patterns',async()=>{const files=await collect();const violations=[];for(const file of files){if(file.relative===SELF)continue;if(file.relative.startsWith('.github/workflows/')&&!ALLOWED_WORKFLOWS.has(file.relative))violations.push(`${file.relative}: workflow is not on the public allowlist`);for(const fragment of forbiddenPathFragments)if(file.relative.includes(fragment))violations.push(`${file.relative}: forbidden operational path category`);for(const pattern of forbiddenPathPatterns)if(pattern.test(file.relative))violations.push(`${file.relative}: forbidden operational path pattern`);const ext=path.extname(file.relative).toLowerCase();if(!TEXT_EXTENSIONS.has(ext)&&!['DCO','Dockerfile','LICENSE','VERSION'].includes(path.basename(file.relative)))continue;let content;try{content=await readFile(file.absolute,'utf8')}catch{continue}const isBenchmarkEvidence=file.relative.startsWith(BENCHMARK_EVIDENCE_DIR);for(const marker of forbiddenLiteralMarkers){if(isBenchmarkEvidence&&marker==='github.com/inn-media/truyn/actions/runs/')continue;if(content.includes(marker))violations.push(`${file.relative}: forbidden operational marker category`)}for(const pattern of forbiddenCredentialPatterns)if(pattern.test(content))violations.push(`${file.relative}: credential/private-key pattern detected`);for(const pattern of forbiddenTopologyPatterns)if(pattern.test(content))violations.push(`${file.relative}: live operational topology pattern detected`);if((file.relative.startsWith('benchmarks/')||file.relative.startsWith('scripts/'))&&EXECUTABLE_EXTENSIONS.has(ext))for(const pattern of forbiddenOperationalExecutablePatterns)if(pattern.test(content))violations.push(`${file.relative}: operational cloud credential/proxy code detected`)}assert.deepEqual(violations,[],`Public repository leakage guard failed:\n${violations.join('\n')}`)});
