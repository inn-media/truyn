diff --git a/benchmarks/scale/class-d-azure-1000-provision.sh b/benchmarks/scale/class-d-azure-1000-provision.sh
--- a/benchmarks/scale/class-d-azure-1000-provision.sh
+++ b/benchmarks/scale/class-d-azure-1000-provision.sh
@@ -58,10 +58,15 @@ remote() {
   remote_script="${remote_script//truyn/truyn}"
   remote_script="${remote_script//truyn/truyn}"
   for attempt in 1 2 3 4 5; do
-    set +e
-    output=$(az vm run-command invoke -g "$RG" -n "$vm" --command-id RunShellScript --scripts "$remote_script" --query 'value[0].message' -o tsv --only-show-errors 2>&1)
-    rc=$?
-    set -e
+    # `set +e` disables errexit but not an inherited ERR trap (`set -E`).
+    # Keep the transient Azure failure in an `if` condition so stage traps cannot
+    # exit the caller before this bounded retry loop records rc and retries it.
+    if output=$(az vm run-command invoke -g "$RG" -n "$vm" --command-id RunShellScript --scripts "$remote_script" --query 'value[0].message' -o tsv --only-show-errors 2>&1); then
+      rc=0
+    else
+      rc=$?
+    fi
     printf '%s\n' "$output" >&2
     if [[ $rc -eq 0 ]]; then
       printf '%s\n' "$output"
diff --git a/benchmarks/scale/class-d-azure-1000-campaign.sh b/benchmarks/scale/class-d-azure-1000-campaign.sh
--- a/benchmarks/scale/class-d-azure-1000-campaign.sh
+++ b/benchmarks/scale/class-d-azure-1000-campaign.sh
@@ -874,7 +874,7 @@ done
 echo WRITES=\$ok
 EOS
 )
-  (remote "${VMS[$i]}" "$script" >"${d200_write_dir}/${i}.out" 2>"${d200_write_dir}/${i}.err") &
+  (trap - ERR; remote "${VMS[$i]}" "$script" >"${d200_write_dir}/${i}.out" 2>"${d200_write_dir}/${i}.err") &
   d200_write_pids+=("$!")
 done
 d200_write_remote_failed=0
diff --git a/benchmarks/scale/d200-post-restart-routing-stage.sh b/benchmarks/scale/d200-post-restart-routing-stage.sh
--- a/benchmarks/scale/d200-post-restart-routing-stage.sh
+++ b/benchmarks/scale/d200-post-restart-routing-stage.sh
@@ -51,6 +51,7 @@ PY
 EOS
 )
   (
+    trap - ERR
     set +e
     remote "${VMS[$i]}" "$script" >"$post_dir/$i.out" 2>"$post_dir/$i.err"
     printf '%s\n' "$?" >"$post_dir/$i.rc"
diff --git a/benchmarks/scale/d200-resources-stage.sh b/benchmarks/scale/d200-resources-stage.sh
--- a/benchmarks/scale/d200-resources-stage.sh
+++ b/benchmarks/scale/d200-resources-stage.sh
@@ -40,6 +40,7 @@ exit 0
 EOS
 )
   (
+    trap - ERR
     set +e
     remote "${VMS[$i]}" "$script" >"$resources_dir/$i.out" 2>"$resources_dir/$i.err"
     printf '%s\n' "$?" >"$resources_dir/$i.rc"
diff --git a/benchmarks/scale/d200-restart-recovery-stage.sh b/benchmarks/scale/d200-restart-recovery-stage.sh
--- a/benchmarks/scale/d200-restart-recovery-stage.sh
+++ b/benchmarks/scale/d200-restart-recovery-stage.sh
@@ -152,6 +152,7 @@ exit 0
 EOS
 )
   (
+    trap - ERR
     set +e
     remote "${VMS[$i]}" "$script" >"$restart_dir/$i.out" 2>"$restart_dir/$i.err"
     printf '%s\n' "$?" >"$restart_dir/$i.remote_rc"
diff --git a/benchmarks/scale/d200-write-retention-stage.sh b/benchmarks/scale/d200-write-retention-stage.sh
--- a/benchmarks/scale/d200-write-retention-stage.sh
+++ b/benchmarks/scale/d200-write-retention-stage.sh
@@ -47,6 +47,7 @@ PY
 EOS
 )
   (
+    trap - ERR
     set +e
     remote "${VMS[$i]}" "$script" >"$retention_dir/$i.out" 2>"$retention_dir/$i.err"
     printf '%s\n' "$?" >"$retention_dir/$i.rc"
diff --git a/network/transport/quic.js b/network/transport/quic.js
--- a/network/transport/quic.js
+++ b/network/transport/quic.js
@@ -1,5 +1,5 @@
 import { createHmac, randomFillSync, randomBytes, timingSafeEqual } from 'node:crypto';
-import { QUICSocket, QUICServer, QUICClient, events } from '@matrixai/quic';
+import { QUICSocket, QUICServer, QUICClient, events, errors as quicErrors } from '@matrixai/quic';
 import { verifyEnvelope } from '../../core/protocol/index.js';
 import { BoundedAdmissionQueue } from '../admission/bounded-queue.js';
 import { createSessionHello, createSessionAccept, verifySessionHello, verifySessionAccept, sessionHandshakeBinding, sessionId, SessionReplayCache } from '../sessions/authenticated-session.js';
@@ -98,9 +98,11 @@ function boundedHandlerError(error, fallback) {
 }
 
 export class TruynQuicTransport {
-  constructor({ identity, host = '0.0.0.0', port = 0, tls, maxMessageBytes = 1_048_576, maxInboundInFlight = 64, maxInboundQueued = 256 } = {}) {
+  constructor({ identity, host = '0.0.0.0', port = 0, tls, maxMessageBytes = 1_048_576, maxInboundInFlight = 64, maxInboundQueued = 256, connectTimeoutMs = 5_000 } = {}) {
     if (!identity?.nodeId || !identity?.publicKeyPem || !identity?.privateKeyPem) throw new Error('QUIC transport identity is required');
     if (!tls?.key || !tls?.cert) throw new Error('QUIC server TLS key and certificate are required');
+    if (!Number.isInteger(connectTimeoutMs) || connectTimeoutMs < 100 || connectTimeoutMs > 120_000) throw new Error('QUIC connectTimeoutMs must be between 100 and 120000');
     this.identity = identity;
     this.host = host;
     this.port = port;
@@ -108,6 +110,7 @@ export class TruynQuicTransport {
     this.tls = tls;
     this.maxMessageBytes = maxMessageBytes;
+    this.connectTimeoutMs = connectTimeoutMs;
     this.socket = new TruynQuicSocket({});
     this.server = null;
     this.clients = new Set();
@@ -222,8 +225,11 @@ export class TruynQuicTransport {
     await writeJson(stream, { ok: false, error: 'unsupported_quic_message' });
   }
 
-  async connect({ host, port, serverName = host, ca = this.tls.ca } = {}) {
-    const client = await QUICClient.createQUICClient({
+  async connect({ host, port, serverName = host, ca = this.tls.ca } = {}, { signal = null, timeoutMs = this.connectTimeoutMs } = {}) {
+    const effectiveTimeoutMs = Number.isFinite(timeoutMs) ? Math.max(1, Math.floor(timeoutMs)) : this.connectTimeoutMs;
+    let client;
+    try {
+      client = await QUICClient.createQUICClient({
       host,
       port,
       serverName,
@@ -238,7 +244,20 @@ export class TruynQuicTransport {
         maxIdleTimeout: 30_000,
         keepAliveIntervalTime: 10_000
       }
-    });
+      }, {
+        timer: effectiveTimeoutMs,
+        ...(signal ? { signal } : {})
+      });
+    } catch (error) {
+      // @matrixai/quic exposes a cancellable/timed create operation. Normalize its
+      // own timeout into the same transient class used by the bounded P2P retry path.
+      if (signal?.aborted && signal.reason instanceof Error) throw signal.reason;
+      if (error instanceof quicErrors.ErrorQUICClientCreateTimeout) {
+        const timeout = new Error('quic_connect_timeout', { cause: error });
+        timeout.code = 'ETIMEDOUT';
+        timeout.transient = true;
+        throw timeout;
+      }
+      throw error;
+    }
     this.clients.add(client);
     const hello = createSessionHello({ identity: this.identity, endpoints: [`quic://${this.host}:${this.port}`] });
     const response = await requestJson(client.connection, { kind: 'session-hello', hello }, this.maxMessageBytes);
diff --git a/network/transport/p2p.js b/network/transport/p2p.js
--- a/network/transport/p2p.js
+++ b/network/transport/p2p.js
@@ -172,15 +172,19 @@ export class DirectFirstP2P {
     const timeoutMs = Math.min(this.directConnectTimeoutMs, this.#remainingMs(deadlineAt));
     if (timeoutMs <= 0) throw routeDeadlineError(peerNodeId, 'direct-connect');
     let timer = null;
     let timedOut = false;
-    const operation = Promise.resolve().then(() => this.quic.connect(endpoint));
+    const controller = new AbortController();
+    const operation = Promise.resolve().then(() => this.quic.connect(endpoint, { signal: controller.signal, timeoutMs }));
     try {
       return await Promise.race([
         operation,
         new Promise((_, reject) => {
           timer = setTimeout(() => {
             timedOut = true;
             const error = new Error(`p2p_connect_timeout:${peerNodeId}`);
             error.code = 'TRUYN_P2P_CONNECT_TIMEOUT';
+            // Cancel the actual native handshake. A Promise.race alone leaves
+            // createQUICClient alive and mass restart turns those late connects into a tail.
+            controller.abort(error);
             reject(error);
           }, timeoutMs);
         })
@@ -188,6 +192,7 @@ export class DirectFirstP2P {
       ]);
     } finally {
       if (timer) clearTimeout(timer);
+      if (timedOut && !controller.signal.aborted) controller.abort();
       if (timedOut) void operation.then((client) => this.#disconnectClient(client)).catch(() => {});
     }
   }
diff --git a/network/discovery/quic-rpc.js b/network/discovery/quic-rpc.js
--- a/network/discovery/quic-rpc.js
+++ b/network/discovery/quic-rpc.js
@@ -136,7 +136,7 @@ export class QuicDiscoveryRpc {
     void this.#disconnect(existing.client);
   }
 
-  async client(peer) {
+  async client(peer, { timeoutMs = null, signal = null } = {}) {
     const selected = selectedEndpoint(peer);
     if (!selected) throw new Error('discovery_peer_has_no_quic_endpoint');
     const binding = peerBinding(peer, selected.value);
@@ -158,7 +158,13 @@ export class QuicDiscoveryRpc {
     }
 
     const state = { binding, peer: structuredClone(peer), promise: null, discarded: false };
     state.promise = (async () => {
-      const client = await this.quic.connect(selected.endpoint);
+      const connectTimeoutMs = this.#effectiveTimeout(timeoutMs);
+      if (connectTimeoutMs <= 0) {
+        const error = new Error(`TRUYN_DHT_RPC_TIMEOUT:${peer.nodeId}`);
+        error.code = 'TRUYN_DHT_RPC_TIMEOUT';
+        throw error;
+      }
+      const client = await this.quic.connect(selected.endpoint, { timeoutMs: connectTimeoutMs, signal });
       if (state.discarded || this.connectingByNodeId.get(peer.nodeId) !== state) {
         await this.#disconnect(client);
         const replacement = this.connectingByNodeId.get(peer.nodeId);
@@ -192,6 +198,7 @@ export class QuicDiscoveryRpc {
     const deadlineAt = Date.now() + effectiveTimeoutMs;
     let supersededAttempt = 0;
     while (true) {
+      const abortController = new AbortController();
       let timer = null;
       if (state) { state.client = null; state.clientLeased = false; state.cancelledError = null; }
       try {
@@ -204,7 +211,7 @@ export class QuicDiscoveryRpc {
           throw error;
         }
         return await Promise.race([
-          operation(),
+          operation({ signal: abortController.signal, timeoutMs: remaining }),
           new Promise((_, reject) => {
             timer = setTimeout(() => {
               const error = new Error(`TRUYN_DHT_RPC_TIMEOUT:${peer.nodeId}`);
               error.code = 'TRUYN_DHT_RPC_TIMEOUT';
               if (state) state.cancelledError = error;
+              abortController.abort(error);
               reject(error);
             }, remaining);
           })
         ]);
       } catch (error) {
-        if (state?.client) this.#retireClient(peer.nodeId, state.client);
+        if (error?.code === 'TRUYN_DHT_RPC_TIMEOUT') this.forget(peer.nodeId);
+        else if (state?.client) this.#retireClient(peer.nodeId, state.client);
         if (error?.code === 'TRUYN_DISCOVERY_CONNECTION_SUPERSEDED' && supersededAttempt < this.supersededRetries && Date.now() < deadlineAt) {
           supersededAttempt += 1;
           continue;
@@ -220,16 +228,16 @@ export class QuicDiscoveryRpc {
     }
   }
 
-  async #leasedClient(peer, state) {
-    const client = await this.client(peer);
+  async #leasedClient(peer, state, connectContext = {}) {
+    const client = await this.client(peer, connectContext);
     this.#acquireClient(client);
     state.client = client;
     state.clientLeased = true;
     if (state.cancelledError) throw state.cancelledError;
     return client;
   }
 
   async ping(peer, options = {}) {
     const state = { client: null, clientLeased: false, cancelledError: null };
-    return this.bounded(peer, async () => {
-      const client = await this.#leasedClient(peer, state);
+    return this.bounded(peer, async (connectContext) => {
+      const client = await this.#leasedClient(peer, state, connectContext);
       const result = await this.quic.requestControl(client, QUIC_DHT_METHOD_PING, null);
       if (verifyPeerRecord(result?.peerRecord).ok && this.ingestPeerRecord) {
         const record = structuredClone(result.peerRecord);
@@ -245,8 +253,8 @@ export class QuicDiscoveryRpc {
 
   async findNode(peer, targetNodeId, options = {}) {
     const state = { client: null, clientLeased: false, cancelledError: null };
-    return this.bounded(peer, async () => {
-      const client = await this.#leasedClient(peer, state);
+    return this.bounded(peer, async (connectContext) => {
+      const client = await this.#leasedClient(peer, state, connectContext);
       const result = await this.quic.requestControl(client, QUIC_DISCOVERY_METHOD_FIND_NODE, { targetNodeId });
       const records = [];
       const hints = [];
@@ -263,8 +271,8 @@ export class QuicDiscoveryRpc {
     const verification = verifyPeerRecord(record);
     if (!verification.ok) throw new Error(`invalid_peer_record:${verification.reason}`);
     const state = { client: null, clientLeased: false, cancelledError: null };
-    return this.bounded(peer, async () => {
-      const client = await this.#leasedClient(peer, state);
+    return this.bounded(peer, async (connectContext) => {
+      const client = await this.#leasedClient(peer, state, connectContext);
       return this.quic.requestControl(client, QUIC_DISCOVERY_METHOD_ANNOUNCE, { record });
     }, { ...options, state });
   }
@@ -274,8 +282,8 @@ export class QuicDiscoveryRpc {
     const verification = verifyDhtRecord(record);
     if (!verification.ok) throw new Error(`invalid DHT record: ${verification.reason}`);
     const state = { client: null, clientLeased: false, cancelledError: null };
-    return this.bounded(peer, async () => {
-      const client = await this.#leasedClient(peer, state);
+    return this.bounded(peer, async (connectContext) => {
+      const client = await this.#leasedClient(peer, state, connectContext);
       return this.quic.requestControl(client, QUIC_DHT_METHOD_STORE, { record });
     }, { ...options, state });
   }
@@ -283,8 +291,8 @@ export class QuicDiscoveryRpc {
 
   async findValue(peer, namespace, key, options = {}) {
     const state = { client: null, clientLeased: false, cancelledError: null };
-    return this.bounded(peer, async () => {
-      const client = await this.#leasedClient(peer, state);
+    return this.bounded(peer, async (connectContext) => {
+      const client = await this.#leasedClient(peer, state, connectContext);
       const result = await this.quic.requestControl(client, QUIC_DHT_METHOD_FIND_VALUE, { namespace, key });
       const records = [];
       for (const record of result?.records || []) {
diff --git a/tests/class-d-remote-errtrace.test.js b/tests/class-d-remote-errtrace.test.js
new file mode 100644
--- /dev/null
+++ b/tests/class-d-remote-errtrace.test.js
@@ -0,0 +1,25 @@
+import test from 'node:test';
+import assert from 'node:assert/strict';
+import { mkdtemp, writeFile, chmod, readFile, rm } from 'node:fs/promises';
+import { tmpdir } from 'node:os';
+import { join } from 'node:path';
+import { spawnSync } from 'node:child_process';
+
+test('Class-D remote retries a transient run-command failure under an inherited ERR trap', async () => {
+  const dir = await mkdtemp(join(tmpdir(), 'truyn-remote-errtrace-'));
+  try {
+    const provision = await readFile('benchmarks/scale/class-d-azure-1000-provision.sh', 'utf8');
+    const remoteFn = provision.match(/^remote\(\) \{[\s\S]*?^\}/m)?.[0];
+    assert.ok(remoteFn, 'remote() must exist in the shared provisioner');
+    await writeFile(join(dir, 'az'), '#!/usr/bin/env bash\nif [[ ! -f "$STATE/failed" ]]; then touch "$STATE/failed"; echo "ERROR: (Conflict) Run command extension execution is in progress." >&2; exit 1; fi\necho OK_MARKER=1\n');
+    await chmod(join(dir, 'az'), 0o755);
+    const script = `${remoteFn}\nsleep() { :; }\nRG=rg\n(\n  trap 'exit 97' ERR\n  set -Eeuo pipefail\n  (remote vm-h0 'true' >"${dir}/out") &\n  wait $! || { echo BACKGROUND_REMOTE_KILLED; exit 1; }\n  grep -q OK_MARKER=1 "${dir}/out"\n  echo STAGE_OK\n)\n`;
+    const run = spawnSync('bash', ['-c', script], { encoding: 'utf8', env: { ...process.env, PATH: `${dir}:${process.env.PATH}`, STATE: dir } });
+    assert.match(run.stdout, /STAGE_OK/, `stdout=${run.stdout} stderr=${run.stderr}`);
+    assert.doesNotMatch(run.stdout, /BACKGROUND_REMOTE_KILLED/);
+  } finally {
+    await rm(dir, { recursive: true, force: true });
+  }
+});
diff --git a/tests/d500-connect-timeout-recovery.test.js b/tests/d500-connect-timeout-recovery.test.js
new file mode 100644
--- /dev/null
+++ b/tests/d500-connect-timeout-recovery.test.js
@@ -0,0 +1,78 @@
+import test from 'node:test';
+import assert from 'node:assert/strict';
+import { readFile } from 'node:fs/promises';
+import { DirectFirstP2P } from '../network/transport/p2p.js';
+import { QuicDiscoveryRpc } from '../network/discovery/quic-rpc.js';
+
+const peer = {
+  nodeId: 'truyn:node:d500-timeout-peer',
+  endpoints: ['quic://127.0.0.1:4433'],
+  sequence: 1
+};
+
+test('direct P2P timeout aborts the underlying connect before relay fallback', async () => {
+  let aborted = false;
+  let relayCalls = 0;
+  const quic = {
+    connect(_endpoint, { signal } = {}) {
+      return new Promise((_resolve, reject) => {
+        signal?.addEventListener('abort', () => {
+          aborted = true;
+          reject(signal.reason || Object.assign(new Error('aborted'), { code: 'ETIMEDOUT' }));
+        }, { once: true });
+      });
+    },
+    async disconnect() {}
+  };
+  const discovery = { get: (nodeId) => nodeId === peer.nodeId ? peer : null };
+  const router = new DirectFirstP2P({
+    quicTransport: quic,
+    discovery,
+    relayFallback: async () => { relayCalls += 1; return { ok: true }; },
+    directConnectTimeoutMs: 20,
+    directConnectAttempts: 1,
+    routeAttemptTimeoutMs: 100
+  });
+  const result = await router.send(peer.nodeId, { type: 'NEED' });
+  assert.equal(result.transport, 'relay-fallback');
+  assert.equal(relayCalls, 1);
+  assert.equal(aborted, true, 'native connect must be cancelled, not left alive behind Promise.race');
+});
+
+test('DHT RPC timeout evicts a stuck in-flight connect so the next operation can reconnect', async () => {
+  let firstResolve;
+  let connectCalls = 0;
+  const disconnects = [];
+  const optionsSeen = [];
+  const first = { id: 'first' };
+  const second = { id: 'second' };
+  const quic = {
+    connect(_endpoint, options = {}) {
+      connectCalls += 1;
+      optionsSeen.push(options);
+      if (connectCalls === 1) return new Promise((resolve) => { firstResolve = resolve; });
+      return Promise.resolve(second);
+    },
+    async disconnect(client) { disconnects.push(client.id); },
+    async requestControl() { return { pong: true }; }
+  };
+  const rpc = new QuicDiscoveryRpc({ quicTransport: quic, timeoutMs: 100 });
+  await assert.rejects(rpc.ping(peer), (error) => error?.code === 'TRUYN_DHT_RPC_TIMEOUT');
+  assert.equal(await rpc.ping(peer), true);
+  assert.equal(connectCalls, 2, 'second operation must not coalesce onto the timed-out connect');
+  assert.equal(optionsSeen[0].timeoutMs, 100);
+  firstResolve(first);
+  await new Promise((resolve) => setTimeout(resolve, 20));
+  assert.ok(disconnects.includes('first'), 'late client from timed-out connect must be destroyed');
+});
+
+test('TRUYN QUIC transport wires the bounded connect context into the cancellable library create', async () => {
+  const source = await readFile(new URL('../network/transport/quic.js', import.meta.url), 'utf8');
+  assert.match(source, /createQUICClient\([\s\S]*timer:\s*effectiveTimeoutMs[\s\S]*signal/);
+  assert.match(source, /ErrorQUICClientCreateTimeout/);
+});
