diff --git a/.github/workflows/d500-acceptance.yml b/.github/workflows/d500-acceptance.yml
--- a/.github/workflows/d500-acceptance.yml
+++ b/.github/workflows/d500-acceptance.yml
@@ -22,6 +22,7 @@ env:
   EXACT_MAIN_CI_RUN: '35719191962'
   EXACT_MAIN_FIVE_PATCH_RUN: '35719191909'
   EXACT_MAIN_CODEQL_RUN: '35719190942'
+  D_SERIES_BLOCKWISE_PREFLIGHT_RUN: '0'
   D500_CONTRACT_SHA256: 469e7354eda2b42e90341c4665aa1dea623cbdaa5b63ab3a8a5889e913fd8f85
   AZURE_RESOURCE_GROUP: truyn
   NODES_PER_HOST: '25'
@@ -62,6 +63,7 @@ jobs:
           [[ "$(value EXACT_MAIN_CI_RUN)" == "$EXACT_MAIN_CI_RUN" ]]
           [[ "$(value EXACT_MAIN_FIVE_PATCH_RUN)" == "$EXACT_MAIN_FIVE_PATCH_RUN" ]]
           [[ "$(value EXACT_MAIN_CODEQL_RUN)" == "$EXACT_MAIN_CODEQL_RUN" ]]
+          [[ "$(value D_SERIES_BLOCKWISE_PREFLIGHT_RUN)" == "$D_SERIES_BLOCKWISE_PREFLIGHT_RUN" ]]
           [[ "$(value D500_CONTRACT_SHA256)" == "$D500_CONTRACT_SHA256" ]]
           expected_workflow_blob="$(value WORKFLOW_BLOB_SHA)"
           [[ "$expected_workflow_blob" =~ ^[0-9a-f]{40}$ ]]
@@ -83,6 +85,8 @@ jobs:
           set -Eeuo pipefail
           [[ "$(git rev-parse HEAD)" == "$TESTED_COMMIT" ]]
           [[ "$(git rev-parse "${TESTED_COMMIT}^{tree}")" == "$TESTED_TREE_SHA" ]]
+          export TRUYN_D_SERIES_BLOCKWISE_PREFLIGHT_RUN="$D_SERIES_BLOCKWISE_PREFLIGHT_RUN"
+          bash scripts/verify-d-series-blockwise-preflight-run.sh "$TESTED_COMMIT"
           ci="$(gh api "repos/${GITHUB_REPOSITORY}/actions/runs/${EXACT_MAIN_CI_RUN}")"
           fp="$(gh api "repos/${GITHUB_REPOSITORY}/actions/runs/${EXACT_MAIN_FIVE_PATCH_RUN}")"
           cq="$(gh api "repos/${GITHUB_REPOSITORY}/actions/runs/${EXACT_MAIN_CODEQL_RUN}")"
diff --git a/.github/d500/d500-acceptance.template.yml b/.github/d500/d500-acceptance.template.yml
--- a/.github/d500/d500-acceptance.template.yml
+++ b/.github/d500/d500-acceptance.template.yml
@@ -26,6 +26,7 @@ env:
   EXACT_MAIN_FIVE_PATCH_RUN: '__EXACT_MAIN_FIVE_PATCH_RUN__'
   QUALIFIED_TREE_CODEQL_CHECK: '__QUALIFIED_TREE_CODEQL_CHECK__'
   QUALIFIED_TREE_CODEQL_SHA: __QUALIFIED_TREE_CODEQL_SHA__
+  D_SERIES_BLOCKWISE_PREFLIGHT_RUN: '__D_SERIES_BLOCKWISE_PREFLIGHT_RUN__'
   D500_CONTRACT_SHA256: __D500_CONTRACT_SHA256__
   AZURE_RESOURCE_GROUP: truyn
   NODES_PER_HOST: '25'
@@ -59,6 +60,7 @@ jobs:
           [[ "$(value EXACT_MAIN_FIVE_PATCH_RUN)" == "$EXACT_MAIN_FIVE_PATCH_RUN" ]]
           [[ "$(value QUALIFIED_TREE_CODEQL_CHECK)" == "$QUALIFIED_TREE_CODEQL_CHECK" ]]
           [[ "$(value QUALIFIED_TREE_CODEQL_SHA)" == "$QUALIFIED_TREE_CODEQL_SHA" ]]
+          [[ "$(value D_SERIES_BLOCKWISE_PREFLIGHT_RUN)" == "$D_SERIES_BLOCKWISE_PREFLIGHT_RUN" ]]
           [[ "$(value D500_CONTRACT_SHA256)" == "$D500_CONTRACT_SHA256" ]]
           expected_workflow_blob="$(value WORKFLOW_BLOB_SHA)"
           [[ "$expected_workflow_blob" =~ ^[0-9a-f]{40}$ ]]
@@ -80,6 +82,8 @@ jobs:
           set -Eeuo pipefail
           [[ "$(git rev-parse HEAD)" == "$TESTED_COMMIT" ]]
           [[ "$(git rev-parse "${TESTED_COMMIT}^{tree}")" == "$TESTED_TREE_SHA" ]]
+          export TRUYN_D_SERIES_BLOCKWISE_PREFLIGHT_RUN="$D_SERIES_BLOCKWISE_PREFLIGHT_RUN"
+          bash scripts/verify-d-series-blockwise-preflight-run.sh "$TESTED_COMMIT"
           ci="$(gh api "repos/${GITHUB_REPOSITORY}/actions/runs/${EXACT_MAIN_CI_RUN}")"
           fp="$(gh api "repos/${GITHUB_REPOSITORY}/actions/runs/${EXACT_MAIN_FIVE_PATCH_RUN}")"
           cq="$(gh api "repos/${GITHUB_REPOSITORY}/check-runs/${QUALIFIED_TREE_CODEQL_CHECK}")"
diff --git a/scripts/class-d-1000-final-acceptance.sh b/scripts/class-d-1000-final-acceptance.sh
--- a/scripts/class-d-1000-final-acceptance.sh
+++ b/scripts/class-d-1000-final-acceptance.sh
@@ -354,5 +354,8 @@ if [[ "${TRUYN_CLASS_D1000_PREPARE_ONLY:-0}" == 1 ]]; then
   exit 0
 fi
 
+export TRUYN_D_SERIES_BLOCKWISE_PREFLIGHT_RUN="${TRUYN_D_SERIES_BLOCKWISE_PREFLIGHT_RUN:-}"
+bash "$ROOT/scripts/verify-d-series-blockwise-preflight-run.sh" "${TESTED_COMMIT:-${GITHUB_SHA:-}}"
+
 source "$TMP/provision.sh"
