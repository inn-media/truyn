# TRUYN Class D-200 Accepted Evidence — 2026-09-20

Status: **PASS — accepted Class D-200 gate**

This is the durable public evidence record for the accepted Class D-200 single-shot campaign. It records the immutable source/launcher/run tuple, unchanged acceptance thresholds, measured topology/routing/recovery/durability/safety/resource telemetry, cleanup closure, artifact identity, and the exact public/private publication boundary.

The public record intentionally follows a **verify-max / exposure-min** rule: publish every sanitized measurement required to independently verify the benchmark, but never publish secrets, credentials, private/internal network addresses, cloud resource identifiers, secret-bearing URLs, connection strings, or unnecessary raw operational logs.

## Immutable identity

- Task: `truyn-d200-parallel-closure-260914-a7f3`
- Durable anchor: issue `#536`
- Accepted workflow run: `35503894414`
- Run attempt: `1`
- Job: `106060322939`
- Launcher merge SHA: `e785815530a59a56787e20ceb6bb232ccc93ad4f`
- Frozen tested source SHA: `e91c165c67c655deb80df4511ca346acb9f1f45b`
- Frozen tested source tree: `3a402ba72502de12ed2277db3c9f472872f44b46`
- Exact-main CI: `35500410155` — SUCCESS
- Exact-main Five-Patch: `35500410293` — SUCCESS
- Qualified-tree CodeQL check: `106050818849` on `293e3cf5da54e58e37a8d3bb3c138b05bf03e36b`, same tested tree — SUCCESS
- Artifact: `truyn-class-d200-e91c165c-35503894414`
- Artifact ID: `10603748497`
- Artifact digest: `sha256:386387165b729ed2167140747a310d85822d9d1987dce812812408f2468bccd4`
- Runtime bundle digest: `sha256:296e7684229eaea00be02ce573b255461e340eae1b07b88da395c0f1102598c3`
- Placement class: Azure `eastus2`, `Standard_E2as_v7`
- Strict terminal marker: `TRUYN_D200_TERMINAL result=PASS`

Run `35503894414` is immutable accepted evidence and **NEVER_RERUN**.

## Canonical acceptance contract

No acceptance threshold was weakened.

| Predicate | Required | Measured | Result |
| --- | ---: | ---: | --- |
| Hosts | 20 | 20 | PASS |
| Real processes | 200 | 200 | PASS |
| Processes per host | 10 | 10 | PASS |
| Unique identities | 200 | 200 | PASS |
| Unique endpoints | 200 | 200 | PASS |
| Synthetic nodes | 0 | 0 | PASS |
| Readiness | >=99% | 200/200 = 100% | PASS |
| Convergence routing | >=99% | 200/200 = 100% | PASS |
| Baseline routing | >=99% | 400/400 = 100% | PASS |
| Post-restart routing | >=99% | 100/100 = 100% | PASS |
| Healed routing | >=99% | 200/200 = 100% | PASS |
| Convergence p95 | <=120,000 ms | 256.43 ms | PASS |
| Restart recovery p95 | <=120,000 ms | 28,717 ms | PASS |
| Packet-partition recovery | <=120,000 ms | 32,159 ms | PASS |
| Acknowledged durable writes | 100 | 100 | PASS |
| Acknowledged write loss | 0 | 0 | PASS |
| Invalid signed state accepted | 0 | 0 | PASS |
| Stale/revoked receipt accepted | 0 | 0 | PASS |
| Unauthorized provider execution | 0 | 0 | PASS |
| Campaign cleanup confirmed | true | true | PASS |
| Campaign resources remaining | 0 | 0 | PASS |
| Staging cleanup confirmed | true | true | PASS |
| Staging resources remaining | 0 | 0 | PASS |
| Campaign rc | 0 | 0 | PASS |
| Evaluator rc | 0 | 0 | PASS |

`maxPeers=32` remained fixed and all-to-all bootstrap remained forbidden.

## Topology and readiness

The campaign used **20 Azure VMs**, **10 real TRUYN processes per host**, **200 real processes**, **200 unique identities**, **200 unique endpoints**, and zero synthetic nodes. Transport was real UDP/QUIC.

Bootstrap used sparse `host-stratified-xor`, not full mesh: 32 bootstrap records per node, all-to-all `false`, 20/20 failure domains represented. The readiness barrier passed **200/200** nodes in `39,636 ms`; `curlFailures=0`, `parseFailures=0`, `predicateFailures=0`, and pending peer-record propagation at accepted readiness was `0`.

Across all 200 readiness observations:

| Metric | Min | p50 | p90 | p95 | p99 | Max | Mean |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| Valid peers | 71 | 160 | 187 | 191 | 194 | 198 | 155.665 |
| Populated buckets | 6 | 8 | 9 | 9 | 11 | 12 | 7.99 |
| Remote hosts | 20 | 20 | 20 | 20 | 20 | 20 | 20 |
| Expired peer records | 0 | 0 | 0 | 0 | 0 | 0 | 0 |

All 200 sanitized per-node observations are published in `CLASS_D_200_2026-09-20_READINESS_PUBLIC.json`, allowing these distributions to be recomputed independently.

## Routing and convergence

Initial convergence: `200/200` probes, routing ratio `1.0`, p95/p99 `256.43 ms`, aggregate wall time `38,132 ms`, using conservative `max-of-host-quantiles` aggregation.

Baseline: `400/400`, first-attempt-only, zero failures, p50 `233.736 ms`, p90 `503.925 ms`, p95/p99 `638.054 ms`.

After restart: `100/100` first-attempt routing, **0 application retries**, all 20 hosts observed, zero transport-error hosts.

After partition/heal: `200/200`, zero failures, p50 `138.482 ms`, p90/p95/p99 `322.473 ms`.

Per-host routing/convergence measurements are preserved in `CLASS_D_200_2026-09-20.json`.

## Restart recovery

The campaign restarted **100 of 200 nodes** while preserving identity/state paths. Every host passed its five-node restart slice.

- hosts passed: `20/20`
- stop p95: `3,743 ms`
- start p95: `30 ms`
- readiness-after-restart p95: `25,148 ms`
- end-to-end recovery p95: **`28,717 ms`**
- observed per-host recovery range: `8,981–29,389 ms`
- pending propagation max at accepted recovery: `0`

All 20 structured per-host restart measurements are published in the normalized telemetry file; raw stdout/stderr is intentionally not public.

## Real packet partition

A real packet-path partition was exercised:

- probes while blocked: `10`
- successful blocked probes: `0`
- real packet path: `true`
- recovery after heal: **`32,159 ms`**

This passed without weakening the 120-second recovery ceiling.

## Durability and safety

The run acknowledged **100 durable writes**. After restart, partition and heal:

- retained: `100/100`
- acknowledged write loss: `0`
- confirmed missing: `0`
- read errors: `0`

All 100 sanitized retention rows are published in `CLASS_D_200_2026-09-20_RETENTION_PUBLIC.json`, so zero-loss can be independently checked rather than inferred from a summary.

Safety evidence:

- invalid signed state accepted: `0`
- malformed signed state rejected: `true`
- rejection: `invalid_dht_record:dht_record_signature`
- stale/revoked receipt accepted: `0`
- unauthorized provider executions: `0`
- provider access denied: `true`

## Resource telemetry

At final resource observation:

- processes: `200/200`
- aggregate node RSS: `38,728,732 KiB` (~`36.93 GiB`)
- mean RSS per process: ~`189.1 MiB`
- measured QUIC/UDP bytes: `11,104,800,196` (~`10.34 GiB`)
- hosts observed: `20/20`
- per-host RSS range: `1,798,460–2,096,160 KiB`

Per-host process/RSS/traffic values are published in `CLASS_D_200_2026-09-20.json`. These are measurements, not additional acceptance thresholds.

## Timing and stage closure

- campaign duration: `2,860,533 ms` (~47.68 min)
- readiness barrier: `39,636 ms`
- convergence aggregate: `38,132 ms`
- restart recovery p95: `28,717 ms`
- packet-partition recovery: `32,159 ms`

All mandatory stages recorded `PASS rc=0`: topology, readiness-barrier, convergence, baseline-routing, invalid-signed-state, local-safety-invariants, durable-writes, restart-recovery, post-restart-routing, packet-partition, healed-routing, write-retention, resources, evidence.

## Cleanup closure

- campaign cleanup confirmed: `true`
- campaign resources remaining: `0`
- finalized by exit trap: `true`
- staging cleanup confirmed: `true`
- staging resources remaining: `0`

The public evidence intentionally does **not** publish the ephemeral staging resource identifier. Only the acceptance-relevant cleanup result is public.

## Strict terminal result

The launcher emitted the strict terminal PASS with baseline `1`, post-restart `1`, healed `1`, convergence routing `1`, convergence p95 `256.43`, recovery p95 `28717`, partition recovery `32159`, acknowledged writes `100`, acknowledged loss `0`, both cleanup gates true/zero, artifact ID `10603748497`, and the artifact digest above. GitHub Actions concluded success only after the strict terminal check passed.

## Public verification set and security boundary

The durable public verification set is:

- `CLASS_D_200_2026-09-20.md` — this acceptance report;
- `CLASS_D_200_2026-09-20.json` — sanitized normalized telemetry with per-host measurements;
- `CLASS_D_200_2026-09-20_READINESS_PUBLIC.json` — all 200 sanitized per-node readiness observations;
- `CLASS_D_200_2026-09-20_RETENTION_PUBLIC.json` — all 100 sanitized durability/retention rows;
- `CLASS_D_200_2026-09-20_ARTIFACT_FILES.sha256` — SHA-256 of all 20 files in the immutable artifact;
- `CLASS_D_200_2026-09-20_PUBLIC_EVIDENCE_INDEX.json` — explicit publication/exclusion map.

Three original artifact files are deliberately **not published as raw bytes**:

1. `class-d-200-diagnostic.log` — contains private/internal network addressing;
2. `class-d-200-runtime-staging-cleanup.txt` — contains an ephemeral infrastructure resource identifier;
3. `class-d-200-restart-recovery-host-output.log` — redundant unstructured operational stdout/stderr; all acceptance-relevant restart values are published structurally.

Their original SHA-256 values remain public in the artifact manifest. This is a transparent exclusion, not evidence deletion or substitution. The published verification set contains no intended secret, credential, private address, private cloud resource name, secret-bearing URL or connection string.

The generic artifact file `class-d-1000-evidence.json` is named by the shared Class-D harness; the accepted D-200 identity is the immutable D-200 workflow/run/source tuple above. No D-1000 acceptance is claimed.

## Historical integrity

Earlier D-200 failures remain failures and are not rewritten by this PASS: `33959493680`, `34411602064`, `34438746312`, `34448411969`, `35462775116`, and `35470182984`. Historical single-shot failures remain non-rerunnable where previously recorded.

## Scope of claim

This closes the **Class D-200 20-host / 200-real-process resilience gate** for frozen tested source `e91c165c67c655deb80df4511ca346acb9f1f45b` under the immutable tuple above.

It does **not** claim Class D-500, Class D-1000, long-duration operational stability, stable/mainnet readiness, or managed-production acceptance. Those remain separate gates.
