# TRUYN Class D-100 Accepted Evidence — 2026-08-22

Status: **PASS — accepted Class D-100 gate**

This report is the durable public evidence record for the accepted TRUYN Class D-100 V17 run. It records the immutable source/preflight/run tuple, canonical acceptance thresholds, measured results, adversarial coverage, safety invariants, cleanup closure, and artifact identity. The temporary GitHub Actions launcher used to execute the accepted run is not part of the durable repository state and is removed by the same evidence commit that adds this report.

## Immutable identity

- Tested source commit: `ea1753ab6eb12f2c7361740d98f3d08a067bd7c5`
- Fresh security-green run for tested source: `32582970511`
- Immutable preflight workflow commit: `0207f6b6bb96155beadec917658efa8667470aff`
- Immutable preflight run: `32583171432`
- Immutable preflight artifact name: `truyn-class-d100-v17-preflight-32583171432`
- Immutable preflight artifact ID: `9478337894`
- Immutable preflight artifact digest: `sha256:5617cbbd535726e4008cde823ad45107ecc5e7263b2f41bf4ad96abefe8edcd8`
- Accepted launcher/workflow commit: `6da7601a08f15a54a7693881c2b132bc24e507b4`
- Accepted workflow run: `32585255651`
- Accepted job ID: `97060541874`
- Accepted artifact name: `truyn-class-d100-v17-32585255651`
- Accepted artifact ID: `9479365227`
- Accepted artifact digest: `sha256:2625e1cbfbf20ad70c7f9e3460fddd82869287c6f87a0920224ccb8e64e28a90`
- Terminal PASS marker: GitHub issue `#204`, `TRUYN CLASS D-100 V17 MAIN-PINNED PASS`
- Seed: `20260818`
- Canonical accepted entrypoint chain: `V13 -> V12 -> V11 -> V10 -> final`

The accepted run marker records `campaign_outcome=success`, `evaluator_rc=0`, and `terminal_rc=0`.

## Canonical acceptance contract

The accepted gate remained unchanged for this run:

| Predicate | Required | Measured | Result |
| --- | ---: | ---: | --- |
| Real processes | 100 | 100 | PASS |
| Distinct identities | 100 | 100 | PASS |
| Distinct QUIC endpoints/sockets | 100 | 100 | PASS |
| Host failure domains | >=4 | 4 | PASS |
| Baseline routing success | >=99% | 200/200 = 100% | PASS |
| Healed routing success | >=99% | 200/200 = 100% | PASS |
| Recovery p95 | <=120,000 ms | 32,090 ms | PASS |
| Convergence p95 | <=120,000 ms | 32,090 ms | PASS |
| Acknowledged durable write loss | 0 | 0 | PASS |
| Invalid signed state accepted | 0 | 0 | PASS |
| Stale/revoked receipt accepted | 0 | 0 | PASS |
| Churn exercised | required | yes | PASS |
| Packet partition exercised | required | yes | PASS |
| Byzantine exercised | required | yes | PASS |
| Sybil pressure exercised | required | yes | PASS |
| Eclipse exercised | required | yes | PASS |
| Collusion exercised | required | yes | PASS |
| Cleanup confirmed | true | true | PASS |
| Remaining run resources | 0 | 0 | PASS |
| Canonical evaluator | PASS | PASS | PASS |
| Strict terminal verifier | PASS | PASS | PASS |

No threshold, evaluator predicate, terminal predicate, or application-envelope retry rule was weakened to obtain this result.

## Topology

The accepted evidence reports:

- `nodeCount=100`
- `realProcessCount=100`
- `syntheticNodeCount=0`
- `hostCount=4`
- `realProcessesPerHost=25`
- `uniqueIdentityCount=100`
- `uniqueEndpointCount=100`
- `uniqueStatePathCount=100`
- transport: real UDP/QUIC over Azure VNet

This is a real-process scale gate, not a synthetic-node simulation.

## Baseline routing

Before adversarial fault injection:

- probes: `200`
- successes: `200`
- routing success: `1.0` / `100%`
- p50 latency: `49.948 ms`
- p90 latency: `53.261 ms`
- p95 latency: `55.675 ms`
- p99 latency: `57.580 ms`
- latency aggregation: `max-of-host-quantiles`

The canonical `>=99%` baseline routing gate passed.

## Adversarial campaign

### Randomized churn

- stopped processes: `8`
- restarted processes: `8`
- identity state preserved: yes
- recovery: `32,090 ms`

### Real packet-path partition and heal

- real packet path: yes
- successes while blocked: `0`
- recovery after heal: `32,076 ms`

### Byzantine replica resistance

- invalid signed state accepted: `0`

### Sybil pressure

- attacker nodes: `33`
- attacker budget fraction: `0.33`

### Eclipse

- exercised: yes
- escaped after heal: yes
- observed eclipse duration: `167.665 ms`

### Collusion

- colluders: `3`
- coordinated valid signed records observed: `3`
- consensus claim accepted: `false`

All adversarial classes required by the accepted D-100 gate were exercised and recorded as passed by the canonical evaluator and strict terminal verifier.

## Healed state and convergence

After fault recovery and convergence readiness:

- probes: `200`
- successes: `200`
- routing success: `1.0` / `100%`
- p50 latency: `30.157 ms`
- p90 latency: `37.664 ms`
- p95 latency: `47.586 ms`
- p99 latency: `53.070 ms`
- durable valid records observed: `1`
- recovery samples: `32,076 ms`, `32,090 ms`
- recovery p95: `32,090 ms`
- canonical convergence p95: `32,090 ms`

The accepted `>=99%` healed routing gate and both `<=120 s` recovery/convergence gates passed.

## Safety invariants

The accepted evidence records:

- acknowledged durable write loss: `0`
- invalid signed state accepted: `0`
- stale or revoked receipt accepted: `0`

For transparency, the evidence field for stale/revoked receipt acceptance identifies its source as the same-tested-commit CI trust-lifecycle tests rather than a separate stale-receipt fault phase inside this cloud campaign. The canonical evaluator and terminal verifier both require this invariant and passed it for the immutable tested commit.

## Resource observations

The evidence additionally records:

- aggregate node RSS: `10,593,440 KiB`
- measured QUIC/UDP bytes: `382,420,527`
- campaign duration: `2,212,966 ms` (~36m 53s)

These are observations from the accepted run, not new acceptance thresholds.

## Cleanup closure

The accepted run did not stop at protocol success. Its exit cleanup completed and the evidence was finalized after cleanup:

- `cleanup.confirmed=true`
- `cleanup.finalizedByExitTrap=true`
- `cleanup.remainingResources=0`

The strict terminal verifier independently required both cleanup confirmation and zero remaining run resources.

## Canonical evaluator result

`benchmarks/scale/evaluate-class-d-evidence.js` returned:

- `ok=true`
- `passed=true`
- `failed=[]`

Every canonical check was `true`, including topology, baseline/healed routing, recovery/convergence, safety, all six adversarial classes, and cleanup.

## Strict terminal result

`benchmarks/scale/verify-class-d-terminal.js` returned:

- `ok=true`
- `failed=[]`

All strict terminal checks were `true`, including canonical evaluator success, 100 real nodes, 100 identities, 100 QUIC sockets, four hosts, baseline/healed routing, recovery/convergence, zero safety violations, adversarial coverage, cleanup, and zero remaining resources.

## Artifact contents

The accepted immutable artifact contains the run evidence bundle:

- `class-d-100-evidence.json`
- `class-d-100-evaluation.json`
- `class-d-100-evaluation.stderr`
- `class-d-100-terminal.json`
- `class-d-100-terminal.stderr`
- `class-d-100-run.log`
- `class-d-100-v17-input-tuple.txt`
- `class-d-100-v17-prerun.txt`
- `class-d-100-v17-summary.txt`

GitHub Actions artifacts may expire; the artifact ID and cryptographic digest above are retained here so the historical accepted run remains auditable after artifact expiry.

## Historical context

`CLASS_D_100_ATTEMPT_2026-08-17.md` remains preserved as negative/unaccepted historical evidence. This accepted V17 record does not rewrite or delete that earlier failure history.

An earlier V17 launcher attempt (`32584760181`) failed before Azure login because of an overly strict launcher-event guard; its campaign was skipped and it was not counted as a D-100 execution. The corrected authorized one-shot accepted run is `32585255651`, recorded above.

## Scope of the claim

This report closes the TRUYN **Class D-100 accepted 100-real-process resilience gate** for tested commit `ea1753ab6eb12f2c7361740d98f3d08a067bd7c5` under the immutable tuple above.

It does **not** claim the later 1,000-real-node / 20-VM scale gate, randomized long-duration operational stability closure, or production/mainnet readiness. Those remain separate subsequent gates.
